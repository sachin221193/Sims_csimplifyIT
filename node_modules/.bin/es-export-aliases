#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elasticsearch-tools/lib/es-export-aliases.js" "$@"
  ret=$?
else 
  node  "$basedir/../elasticsearch-tools/lib/es-export-aliases.js" "$@"
  ret=$?
fi
exit $ret
